# ระบบบริหารจัดการงานค้าง - สำนักงานที่ดินจังหวัดอ่างทอง
## (DOL Ang Thong Pending Work Management System)

---

## ภาพรวมระบบ

ระบบเว็บแอปพลิเคชันสำหรับบริหารจัดการงานค้างของสำนักงานที่ดินจังหวัดอ่างทอง ออกแบบเพื่อติดตาม ตรวจสอบ และรายงานสถานะงานของ 3 ฝ่ายหลัก พัฒนาด้วย HTML/CSS/JavaScript (Frontend) และ PHP/SQLite (Backend)

---

## โครงสร้างผู้ใช้งาน

### ระบบ Login
- ใช้ชื่อผู้ใช้และรหัสผ่าน
- Session หมดอายุอัตโนมัติหลัง **4 ชั่วโมง**
- Auto logout เมื่อไม่มีการใช้งาน **15 นาที**
- แสดงข้อมูลผู้ใช้ที่มุมขวาบน

### ระดับสิทธิ์ผู้ใช้
| ระดับ | สิทธิ์การเข้าถึง |
|-------|-----------------|
| **admin** | เข้าถึงได้ทุกฝ่าย |
| **survey** | เฉพาะฝ่ายรังวัด |
| **registration** | เฉพาะฝ่ายทะเบียน |
| **academic** | เฉพาะกลุ่มงานวิชาการ |

---

## โมดูลหลักของระบบ

### 1. Dashboard (ภาพรวม)
- แสดงสถิติรวมแยกตามฝ่าย
- Card แสดง: งานทั้งหมด, งานเสร็จ, งานค้างเกิน 30 วัน, งานค้างเกิน 60 วัน
- รายการงานล่าสุด
- รายการงานเร่งด่วน (เกินกำหนด)
- กดที่ Card เพื่อ filter ไปยังรายการนั้นๆ

### 2. งานฝ่ายรังวัด (Survey)
**ข้อมูลที่บันทึก:**
- ลำดับรับ (Auto-generated)
- วันที่รับเรื่อง
- ประเภทการรังวัด (รังวัดสอบเขต, รังวัดแบ่งแยก, ฯลฯ)
- ผู้ขอรังวัด
- สรุปเรื่อง
- สาเหตุที่ค้าง
- คนคุมเรื่อง (ช่างรังวัด)
- วันที่เสร็จ

### 3. งานฝ่ายทะเบียน (Registration)
**ข้อมูลที่บันทึก:**
- เลขที่ (Auto-generated)
- วันที่รับเรื่อง
- เรื่อง
- ผู้เกี่ยวข้อง
- สรุปเรื่อง
- สาเหตุที่ค้าง
- ผู้รับผิดชอบ
- วันที่เสร็จ

### 4. งานกลุ่มงานวิชาการ (Academic)
**ข้อมูลที่บันทึก:**
- เลขที่ (Auto-generated)
- วันที่รับเรื่อง
- เรื่อง
- ผู้เกี่ยวข้อง
- สรุปเรื่อง
- สาเหตุที่ค้าง
- ผู้รับผิดชอบ
- วันที่เสร็จ

### 5. รายงาน (Report)
- สรุปสถิติรายฝ่าย
- ตารางสรุปแยกตามช่วงเวลา (1-30 วัน, 31-60 วัน, เกิน 60 วัน)
- Export เป็น PDF (รูปแบบทางราชการ พร้อมครุฑ)
- Export เป็น Excel

---

## ฟีเจอร์หลัก

### การจัดการข้อมูล
1. **เพิ่มงาน** - กดปุ่ม "+ เพิ่มงาน" บนแต่ละหน้า
2. **ดูรายละเอียด** - กดปุ่ม "ดู" ที่แต่ละแถว
3. **แก้ไขสถานะ** - ในหน้ารายละเอียด สามารถ:
   - ระบุสาเหตุที่ค้าง
   - ระบุวันที่เสร็จ
   - กดปุ่ม "เสร็จสิ้นวันนี้" เพื่อบันทึกเสร็จทันที
4. **ลบงาน** - กดปุ่มสีแดง "ลบงาน" ในหน้ารายละเอียด (มี popup ยืนยัน)
5. **Import ข้อมูล** - นำเข้าจากไฟล์ Excel/CSV

### ตาราง DataTables
- **ค้นหา** - กล่องค้นหา realtime
- **เรียงลำดับ** - คลิก header เพื่อ sort
- **เลือกจำนวนแสดง** - แสดง 10/20/50/100 รายการต่อหน้า
- **Pagination** - เลือกหน้าได้

### การแสดงสถานะ
| สัญลักษณ์ | ความหมาย |
|----------|----------|
| 🚨 สีแดง กระพริบ | ค้างเกิน 60 วัน (วิกฤต) |
| ⚠️ สีส้ม | ค้างเกิน 30 วัน (ต้องติดตาม) |
| ⏳ สีเหลือง | ค้างเกิน 14 วัน |
| สีเขียว | งานปกติ หรือ เสร็จแล้ว |

### ตัวกรอง
- **งานคงค้าง** / **งานที่เสร็จแล้ว** - สลับดูได้
- **กรองตามประเภท** - เลือกประเภทงาน
- **กรองตามเรื่อง** - เลือกหัวข้อเรื่อง

### ประเภทความก้าวหน้า (Progress Type)
| ประเภท | คำอธิบาย |
|--------|----------|
| งานปกติ | งานทั่วไป |
| งานสุดขั้นตอน | รอเอกสารเพิ่มเติม |
| งานระหว่างดำเนินการ | อยู่ระหว่างการทำ |
| งานค้าง | งานที่ยังไม่ได้ดำเนินการ (เกิน 30 วัน) |

---

## เมนูหลัก

1. **ภาพรวม (Dashboard)** - หน้าสรุปภาพรวม
2. **งานฝ่ายรังวัด** - รายการงานฝ่ายรังวัด
3. **นำเข้างานรังวัด** - Import ไฟล์ Excel/CSV
4. **งานฝ่ายทะเบียน** - รายการงานฝ่ายทะเบียน
5. **นำเข้างานทะเบียน** - Import ไฟล์ Excel/CSV
6. **งานกลุ่มงานวิชาการ** - รายการงานวิชาการ
7. **นำเข้างานวิชาการ** - Import ไฟล์ Excel/CSV
8. **รายงาน (Report)** - สรุปรายงานและ Export

---

## การนำเข้าข้อมูล (Import)

### รองรับรูปแบบไฟล์
- Excel (.xlsx)
- CSV (.csv)

### ขั้นตอน
1. กดเมนู "นำเข้าข้อมูล" ของฝ่ายนั้นๆ
2. ดาวน์โหลด Template ตัวอย่าง
3. กรอกข้อมูลลงใน Template
4. Upload ไฟล์
5. ระบบจะ Auto-generate ลำดับ/เลขที่ให้อัตโนมัติ
6. งานที่รับเกิน 30 วัน จะถูกตั้งเป็น "งานค้าง" อัตโนมัติ

---

## เทคโนโลยีที่ใช้

| ส่วน | เทคโนโลยี |
|------|-----------|
| Frontend | HTML5, TailwindCSS, JavaScript ES6+ |
| UI Components | FontAwesome Icons, SweetAlert2, AOS Animation |
| Data Table | DataTables.js + jQuery |
| Backend | PHP 7.4+ |
| Database | SQLite |
| PDF Export | jsPDF, jspdf-autotable |
| Excel Export | SheetJS (xlsx) |
| Web Server | Apache (XAMPP) |

---

## โครงสร้างไฟล์

```
emerald-solstice/
├── index.html          # หน้าหลัก
├── js/
│   ├── app.js          # Logic หลัก
│   ├── ui.js           # UI Components
│   └── data.js         # Data Manager
├── api/
│   ├── db.php          # Database connection
│   ├── survey.php      # API งานรังวัด
│   ├── registration.php# API งานทะเบียน
│   ├── academic.php    # API งานวิชาการ
│   ├── import.php      # API นำเข้าข้อมูล
│   ├── template.php    # Download template
│   └── status_history.php # ประวัติสถานะ
└── database.sqlite     # ฐานข้อมูล
```

---

## API Endpoints

| Method | Endpoint | คำอธิบาย |
|--------|----------|----------|
| GET | /api/survey.php | ดึงข้อมูลงานรังวัด |
| POST | /api/survey.php | เพิ่มงานรังวัด |
| PUT | /api/survey.php | แก้ไขงานรังวัด |
| DELETE | /api/survey.php | ลบงานรังวัด |
| GET | /api/registration.php | ดึงข้อมูลงานทะเบียน |
| POST | /api/registration.php | เพิ่มงานทะเบียน |
| PUT | /api/registration.php | แก้ไขงานทะเบียน |
| DELETE | /api/registration.php | ลบงานทะเบียน |
| GET | /api/academic.php | ดึงข้อมูลงานวิชาการ |
| POST | /api/academic.php | เพิ่มงานวิชาการ |
| PUT | /api/academic.php | แก้ไขงานวิชาการ |
| DELETE | /api/academic.php | ลบงานวิชาการ |
| POST | /api/import.php | นำเข้าข้อมูลจากไฟล์ |
| GET | /api/template.php | ดาวน์โหลด Template |
